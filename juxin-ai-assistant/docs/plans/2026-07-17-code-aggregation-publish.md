# 待提交代码汇总与发布记录

## 目标

汇总当前工作树中已完成的源码、测试、文档和设计资产，在不改变系统版本的前提下完成一次可审计的提交与推送。

## 提交范围

- Agent 市场页面重设计及对应组件测试、端到端测试和样式。
- 聊天 Markdown 表格渲染及回归测试。
- AI 助手原型、工程规范、实施方案、运行手册、开发记忆和架构图。
- 版本自动化测试维护及本地运行产物的忽略规则。

## 安全边界

- 不提交本地模型、Git LFS 临时对象、本地数据库、浏览器日志和测试截图。
- 不提交包含用户标识、请求和响应数据的渠道 outbox。
- 不提交未入选的 Logo 草稿或本机自动生成的 Git LFS hooks。
- 不使用 `git add -A` 或 `git add -f`，仅显式暂存经审计的项目文件。

## 版本边界

用户明确要求本次提交不改变系统版本。聚信 AI 助手保持 `5.3.0`，提交时仅针对本次授权跳过自动升版钩子，并由人工执行推送。

## 验证结果

- 桌面端 TypeScript 类型检查通过。
- Agent 市场与聊天页面定向测试：2 个测试文件、56 条测试通过。
- Agent 市场 Playwright：2 条流程通过。
- AI 助手 4.0 原型：4 条测试及 JavaScript 语法检查通过。
- 版本自动化：56 条测试通过。
- `git diff --check` 通过。
- 高置信密钥与凭据扫描无命中。
