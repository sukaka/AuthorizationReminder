# 2026-07-27 项目工作空间中文化与聊天弹窗

## 用户目标

- 将项目工作空间中的用户可见英文标题、说明和枚举值改为中文。
- 将“项目对话”从项目页签中移出，改为由项目页头入口打开的独立大尺寸弹窗，避免挤压项目任务、资料和交付物区域。

## 实施范围

1. 调整项目工作空间页签、页头和概览卡片的中文文案，并将问题风险、成员角色等原始枚举值显示为中文。
2. 新增仅绑定当前项目的聊天弹窗；支持关闭、Esc 关闭和关闭后回到触发按钮。
3. 移除扩展面板中嵌入式聊天分支，保留项目初始化、资料与知识、项目记忆和成员权限四个项目配置页签。
4. 将扩展面板中的标题和常见项目枚举值中文化。
5. 补充项目页交互测试，验证聊天不再是页签、入口会打开弹窗且保留项目上下文。

## 约束

- 不改后端 API、数据库、权限和项目数据契约。
- 不触碰工作区中与本任务无关的未跟踪文件。
- 本次仅完成本地开发和验证；除非用户另行授权，不提交、不推送、不改版本号。

## 验证计划

- 运行项目工作空间相关 Vitest 测试。
- 运行桌面端 TypeScript 检查。
- 运行 `git diff --check` 检查空白与补丁格式。

## 实施状态

- 已完成：项目页中的系统标题、状态、成员角色和扩展面板常见枚举均改为中文展示；用户录入内容、文件名和内部标识保持原样。
- 已完成：移除“项目对话”页签，在项目页头增加“项目聊天”入口；聊天以当前项目上下文在独立大尺寸弹窗中打开，支持关闭、Esc 关闭和焦点返回。
- 已完成：补充项目聊天弹窗的交互测试。
- 验证通过：`npm run test -- tests/project-workspace-page.test.tsx`（5 个测试）与 `npm run typecheck`；`git diff --check` 无问题。
