# AI 助手审计问题全量修复

日期：2026-07-28
状态：已完成

## 用户目标

修复 AI 助手审计中发现的全部 Bug 和 UI/UX 不合理问题。

## 已确认问题

- 模型任务取消后仍可能继续记录完成事件并提交完成结果。
- 结果同步函数吞掉错误，Word 导出会错误显示“结果已同步”。
- 迁移演练脚本仍固定在 `0066_skill_uploads`，仓库当前 head 为 `0067_project_member_usernames`。
- 多个页面直接使用浏览器原生 `prompt` / `confirm`。
- 聊天输入区缺少键盘焦点反馈。
- 页面全部静态导入，生产主包偏大。
- 测试存在未处理请求噪声，项目成员测试断言已过期。
- 侧边栏图标风格混杂，部分高频控件尺寸和文字偏小，项目成员区标题重复。

## 约束

- 保留工作区已有未提交改动，不回退用户内容。
- 不改变大师 PPT 当前确认提示、图片预览和显式直出逻辑。
- 本次先完成代码与测试，不自动执行版本升级、提交或推送。

## 完成结果

- 修复任务取消竞态：取消后不再记录 `MODEL_COMPLETED`，也不会继续完成、失败上报或保存结果；修订生成同样可被停止。
- 同步队列返回成功、失败和待处理统计；Word 导出遇到同步失败时阻止导出并提示重试。
- 迁移演练和发布门禁更新到 `0067_project_member_usernames`。
- 新增统一应用内弹窗，替换业务代码及测试中的 `window.prompt` / `window.confirm`，支持键盘操作、Escape、焦点恢复和危险操作提示。
- 恢复聊天输入区焦点样式，统一侧边栏线性图标、高频控件尺寸和辅助文字可读性，删除项目成员区重复标题。
- 业务页面改为路由级懒加载；生产主入口由约 912.35 kB 降至 362.50 kB，未再出现超大业务包警告。
- 补齐基础模拟接口和回归测试，消除本轮审计定位的未处理请求噪声。
- 保持大师 PPT 的生成前确认、主题图片提示和显式直出逻辑不变。

## 验证结果

- `npm run typecheck`：通过。
- `npm test -- --reporter=dot`：45 个测试文件、356 项测试全部通过。
- `npm run build:web`：通过；页面已拆分为独立按需加载资源。
- `python3 -m pytest -q tests --ignore=tests/test_migrations.py -ra`：1306 项通过，10 项因可选依赖未安装跳过。
- `git diff --check`：通过。
- 业务代码和测试中无 `window.prompt` / `window.confirm` 残留。

## 交付说明

- 当前版本保持 `5.17.0`，未进行版本升级。
- 本轮未创建 Git 提交，也未推送远端。

## 后续上游集成

- 已在本地快进到 `1bd41719`（`fix(ai-assistant): stabilize generation and access control`），保留本轮工作并解决 `App.tsx` 单一冲突。
- 上游将“AI 能力”限制为管理员可见；测试已相应调整为验证普通员工隐藏、管理员可访问，未削弱权限策略。
- 同步更新 Nginx 代理断言和模型配置接口的 MSW 默认桩。
- 集成后 `npm run typecheck`、相关 3 个测试文件 10 项测试通过；完整测试的终端回传在汇总前截断，未产生失败报告。
