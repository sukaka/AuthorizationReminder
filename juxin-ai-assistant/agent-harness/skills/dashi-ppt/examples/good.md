# 合格示例

## 输入

请把本季度客户流失原因整理成给管理层汇报的 PPT，重点说明原因、影响和下季度动作。我会提供 CRM 导出的 xlsx。

## 合格输出要点

- 自动补全“管理层”为受众，但在 `assumptions` 中说明。
- 使用 xlsx 作为来源，区分数据事实和管理建议。
- 先给出 8–10 页叙事大纲，每页一个结论。
- `html` 为 `planned` 或 `ready`；`pptx`、`pdf` 在未配置授权运行时时为 `blocked`。
