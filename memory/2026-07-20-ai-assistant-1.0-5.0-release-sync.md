# AI 助手 1.0 / 5.0 发布同步记录

## 目标

- 将当前工作区中已经完成的聊天自动路由、知识库统一、安全加固、RAG 评测、运行时和大势 PPT Skill 相关修改整理成可验证提交。
- 推送到 `codex/ai-assistant-5.0`。
- 将 1.0 仍具备的共同能力以兼容方式同步到 `codex/ai-assistant-1.0`，不把 5.0 专属架构强行下放到 1.0。
- 排除本地数据库、运行日志、缓存和密钥。

## 当前基线

- 当前工作分支：`codex/ai-assistant-5.0`。
- 本地基线：`421471ce`。
- 远程 5.0 最新提交：`7b72ea65`，版本 `5.4.1`，包含大势 PPT Skill 依赖补全。
- 远程 1.0 最新提交：`13119c55`，版本 `1.2.0`，包含大势 PPT Skill 运行时接入。
- 当前本地修改尚未提交，且版本镜像曾回落到 `5.3.0`，必须先与远程对齐。

## 执行原则

1. 先合入远程 5.0 最新提交并解决重叠文件。
2. 运行后端、桌面端、H5 和版本一致性检查。
3. 以功能优化提交触发 5.0 次版本升级，目标为 `5.5.0`。
4. 在临时 worktree 中从远程 1.0 最新基线开始做兼容移植。
5. 1.0 只接收其现有架构能承载的共同功能与安全修复，目标版本为 `1.3.0`。
6. 两条分支均在推送前复查状态、提交内容和版本镜像。

## 不进入版本库的文件

- 根目录及 AI 助手目录下的 SQLite 开发数据库。
- `channel_outbox.jsonl` 运行日志。
- `.env`、密钥、缓存、构建输出。
