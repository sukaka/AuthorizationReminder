## 今日对话摘要
- 继续推进“聚信 AI 助手桌面端”开发与恢复验证。用户重点追问 macOS 版是否可用、启动路径、白屏原因、是否必须 HTTPS、局域网 HTTP 调试、Agent 独立版本从 1.0.0 起、自动更新增量包与服务端测试推送接口。
- 今日继续处理 macOS Agent 启动页与统一登录问题：服务端和统一认证均已启动，但点击“使用统一登录”后曾回到“无法连接远程服务”。
- 结论：不是统一认证未开启；业务 API `http://localhost:5193` 和统一登录 `http://localhost:5180/portal?system=ai-assistant` 都可访问。问题在桌面端 WebView 登录窗口加载策略与 macOS HTTP WebView 策略。
- 后续用户反馈“统一登录系统登录后仍显示无法连接远程服务”。继续排查后确认第二个根因：登录成功后统一认证跳转到 AI 助手 Web 工作台 `http://localhost:18093`，但桌面 native 只信任 API origin `http://localhost:5193` 与认证 origin `http://localhost:5180`，导致 Web 工作台跳转/IPC 被误判失败。

## 今日产出/进展
- 已启动并验证本地所需服务：MySQL、auth、prompt-center-api、prompt-center-ai-seed、ai-assistant-db-init、ai-assistant-api、web-ai-assistant。`/api/ai/desktop/bootstrap` 返回统一认证入口。
- 已修复 MySQL 迁移中 Text 字段默认值问题并提交：`5c5c27c2 [v5.90.2] fix(ai-assistant): support mysql desktop update migration`。
- 已新增跨平台 Tauri 打包脚本并提交：`f62e57a8 [v5.90.1] fix(ai-assistant): package lan-test desktop builds`。
- 已修复统一登录打开问题并提交：`e50c1287 [v5.90.3] fix(ai-assistant): open unified login in lan desktop`。
- 已修复统一登录成功后的工作台跳转/IPC 信任问题并提交：`7e14a581 [v5.90.4] fix(ai-assistant): trust desktop workspace web origin`。
- 本批修复登录页与敏感确认体验，已提交：`54d99e60 [v5.90.5] fix(ai-assistant): polish launcher copy and sensitive findings`：
  - 启动页左侧介绍整体上移，避免主视觉贴近垂直居中偏低。
  - 启动页统计文案从“八类助手，88 项常用任务”改为当前 catalog 的“十类助手，258 项常用任务”。
  - 敏感确认弹窗不再展示 `blank_slot_05` 等内部字段名，改为任务字段 label；空输入被后端误报时不再在弹窗里显示。
  - 已重新打包并真实打开 `.app` 检查，启动页显示“十类助手，258 项常用任务”。
- 本批修复 Word 导出按钮“点了没反应”，已提交：`d89bae72 [v5.90.6] fix(ai-assistant): save word exports through desktop shell`：
  - 根因：前端原来依赖浏览器 `<a download>` 触发 Blob 下载，Tauri 桌面 WebView 下不可靠且无成功落点反馈。
  - 桌面端改为下载 docx 后调用 native `generation_word_save`，由 Tauri 保存到系统下载目录。
  - 页面导出成功后显示明确路径：`Word 已保存到：...`；浏览器兜底仍显示“Word 下载已开始”。
  - 新增 Tauri 权限 `allow-generation-word-save`，仅受信任业务 workspace 可调用，并保留 `guard_business` 校验。
- 本批修复首页/模型密钥文案和 macOS 钥匙串弹窗问题，已提交：`4dce02ad [v5.90.7] fix(ai-assistant): store model secrets in encrypted local vault`：
  - 首页蓝色横幅从“八类助手...”改为“十类助手已经准备好结构、提示词与输出要求。”
  - 启动页模型密钥说明从“系统钥匙串”改为“加密保存在本机”。
  - 任务页/模型配置页不再展示“为什么会访问钥匙串？”说明。
  - 运行时 SecretStore 从 macOS Keychain 切到本地加密 vault：使用 AES-256-GCM 加密，secret id 哈希后落盘，文件权限限制当前用户读写。
  - 代价：如果用户删除本机应用数据目录，API Key 需要重新填写；但正常使用不会再触发 macOS 钥匙串授权弹窗。
- 本次 `v5.90.3` 改动包含：
  - 工作台窗口初始加载统一认证 `authPortalUrl`，不再先打开业务后端根路径 `/`。
  - 内网测试/开发构建为 macOS 合并 `Info.lan-test.plist`，允许 WebView 加载受控 HTTP 登录页；生产构建不启用该例外。
  - 增加测试覆盖：窗口初始加载统一认证页、非生产构建才启用 macOS WebView ATS 例外。
- 本次 `v5.90.4` 改动包含：
  - 服务端 desktop bootstrap 增加 `workspaceUrl`，明确告诉桌面端登录成功后的 Web 工作台 origin。
  - 桌面 probe 解析并校验 `workspaceUrl`，拒绝带路径、查询、fragment、userinfo、wildcard 或不符合当前构建模式的地址。
  - native 信任边界拆分为 API origin 与 workspace Web origin：本地数据归属/绑定验证继续使用 API origin，WebView 导航和业务 IPC 改用 workspace Web origin。
  - 增加测试覆盖：bootstrap `workspaceUrl`、probe 解析、登录后 workspace 导航、业务 IPC 不再使用 API origin 授权。
- 真实 UI 验证：重新打包并打开 `/Users/zhanglei/.codex/worktrees/29dc/codex-new/juxin-ai-assistant/apps/desktop/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/聚信 AI 助手.app` 后，点击“使用统一登录”已进入“聚信统一登录平台”登录页。
- 真实 UI 验证：用户手动完成验证码登录后，桌面端已进入 `http://localhost:18093/?portal_session=...` 的 AI 助手工作台，显示“全部助手”和 88 项任务，不再退回“无法连接远程服务”。
- 生成的 DMG 路径：`/Users/zhanglei/.codex/worktrees/29dc/codex-new/juxin-ai-assistant/apps/desktop/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/聚信 AI 助手_1.2.0_aarch64.dmg`。
- 验证命令已通过：
  - `/opt/homebrew/bin/node --test juxin-ai-assistant/apps/desktop/scripts/tests/build-mode.test.mjs`
  - `/Users/zhanglei/.cargo/bin/cargo test --test window_security`
  - `/Users/zhanglei/.cargo/bin/cargo test --test server_config`
  - `juxin-ai-assistant/server/.venv/bin/python -m pytest tests/test_desktop_bootstrap.py -q`
  - `/Users/zhanglei/.cargo/bin/rustfmt --edition 2021 --check ...`
  - `tauri:build:lan-test -- --target aarch64-apple-darwin`
  - `/opt/homebrew/bin/node ./node_modules/vitest/vitest.mjs run tests/task-run.test.tsx tests/launcher-page.test.tsx`
  - `/opt/homebrew/bin/node ./node_modules/vitest/vitest.mjs run tests/task-run.test.tsx`
  - `/Users/zhanglei/.cargo/bin/cargo test file_export_commands --lib`
  - `/Users/zhanglei/.cargo/bin/cargo test local_secret_store --lib`
  - `/Users/zhanglei/.cargo/bin/rustfmt --edition 2021 --check src/file_export_commands.rs`
  - `/Users/zhanglei/.cargo/bin/rustfmt --edition 2021 --check src/keychain.rs`
  - `/opt/homebrew/bin/node ./node_modules/vitest/vitest.mjs run tests/task-run.test.tsx tests/model-profiles.test.tsx tests/employee-pages.test.tsx tests/launcher-page.test.tsx`
  - `/opt/homebrew/bin/node /opt/homebrew/lib/node_modules/npm/bin/npm-cli.js run build`
  - `/usr/bin/arch -arm64 /opt/homebrew/bin/node /opt/homebrew/lib/node_modules/npm/bin/npm-cli.js run tauri:build:lan-test -- --target aarch64-apple-darwin`
  - 最终 `.app` Info.plist 包含 `NSAllowsArbitraryLoadsInWebContent=true`、`NSAllowsLocalNetworking=true`、`NSAllowsArbitraryLoads=false`。

## 风险/阻塞
- GitHub 推送仍未成功：本批 `v5.90.7` 自动 push 失败，当前阻塞为 PAT 缺少 `workflow` scope，无法创建/更新 `.github/workflows/ai-assistant-desktop.yml` 所在分支历史。需要用户更换/授权具有 `workflow` scope 的 GitHub PAT，或让具备权限的身份执行 push。
- 当前分支：`codex/5.90.7`；最新本地提交：`4dce02ad [v5.90.7] fix(ai-assistant): store model secrets in encrypted local vault`。
- 工作区仍有用户/其它任务未提交改动，已刻意避开：
  - `juxin-ai-assistant/apps/desktop/src-tauri/Cargo.lock`
  - `juxin-ai-assistant/apps/desktop/src-tauri/Cargo.toml`
  - `juxin-ai-assistant/apps/desktop/src-tauri/src/commands.rs`
  - `juxin-ai-assistant/apps/desktop/src-tauri/src/model_client.rs`
  - `juxin-ai-assistant/apps/desktop/src-tauri/src/model_profile_store.rs`
  - `juxin-ai-assistant/apps/desktop/src/pages/ModelProfilesPage.tsx`
  - `juxin-ai-assistant/server/app/desktop_update_public.py`
  - `juxin-ai-assistant/server/app/main.py`
  - 未跟踪：`juxin-ai-assistant/apps/desktop/src-tauri/entitlements.plist`
  - 未跟踪：`juxin-ai-assistant/server/juxin-ai-assistant-dev.db`

## 明日计划
- 用户如继续，可从 AI 助手工作台内选择任务，验证动态表单、敏感确认、生成结果反馈、历史详情、设备草稿和待同步流程。
- 继续推进员工工作台功能：八类助手搜索收藏、动态表单、敏感确认、生成结果反馈、历史详情、设备草稿、待同步。
- 继续处理 prompt 手工导入与 Word 导出计划中的剩余任务，保持 TDD 和子代理并行策略（仅在用户明确要求 subagent 时使用）。
- 解决远端推送阻塞：需要用户更换/授权具有 `workflow` scope 的 GitHub PAT，或让具备权限的身份执行 push。

## 可沉淀为长期记忆
- 用户要求桌面 Agent 版本独立于后端版本，从 `1.0.0` 开始；提交/自动更新版本需与版本号规则对应。
- macOS 内网测试版允许私有 IP/localhost HTTP 和 HTTPS；生产版仍应坚持 HTTPS。
- 桌面端即使连不上服务，也必须展示默认启动/登录页和软件简介，并允许登录前手动填写远程地址。
- 自动更新应由客户端拉取服务端已发布的增量/升级包，服务端不负责编译客户端；客户端提示更新并自动重启完成升级。
- 不要读取旧聊天历史；恢复工作优先依据 repo、memory、git 状态和用户明确说明。
