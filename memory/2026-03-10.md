## 今日对话摘要

- 前半段围绕 `tender 3.3 企业资料匹配器` 反馈闭环收尾，重点排查 `tender-api` 容器源码与本地不一致的问题。
- 后半段恢复会话 `019cd1fd-6a9e-7620-9fc3-b74f056012ce`，继续 `train-exam` 管理员结果中心中断前的实现与验证。
- 已确认今天两条主线互不冲突：`tender` 这边 blocker 回到真实 embedding 服务替换，`train-exam` 这边则是管理员结果中心落地与运行态更新。
- 晚些时候新增一条 `faq` 小修：分类管理补齐单个删除与批量删除，删除规则明确为“有关联 FAQ 或子分类时禁止删除”。
- 随后又补了一条 `faq` 前端小改：FAQ 列表和回收站列表增加按分页全局连续显示的序号列。

## 今日产出/进展

- `tender`：
  - 重新核查 `docker-compose.yml` 与 `tender/backend/Dockerfile` 构建链路，确认 build context 仍指向 `./tender/backend`。
  - 对 `tender-api` 做了强制 `--no-cache` 重建与重启，容器内 `/app/src/index.js` 已与本地一致。
  - 正式 `5187` 容器链路下，`persist parse match feedback` 定向 smoke 通过。
- `train-exam`：
  - 恢复并核对管理员结果中心实现，当前改动核心集中在 `train-exam/backend/src/index.js`、`train-exam/backend/src/result-center-utils.js`、`train-exam/frontend/src/App.jsx`、`train-exam/frontend/src/App.css`。
  - 已接通管理员结果列表、卷面详情聚合、考生历史记录查询，以及普通用户“成绩与证书”拆分入口。
  - 已完成最小与扩展验证：
    - `cd train-exam/backend && npx vitest run tests/result-center-utils.test.js` 通过
    - `node --check train-exam/backend/src/index.js` 通过
    - `cd train-exam/frontend && npm run build` 通过
    - `cd train-exam/backend && npx vitest run tests/result-center-utils.test.js tests/question-filter-utils.test.js tests/question-category-utils.test.js tests/paper-rule-utils.test.js tests/question-import-utils.test.js` 通过，`17/17`
  - 已重建并重启 Docker 服务：
    - `train-exam-api`：`5188`
    - `web-train-exam`：`8087`
- `faq`：
  - 新增分类删除设计文档与实现计划：`docs/plans/2026-03-10-faq-category-delete-design.md`、`docs/plans/2026-03-10-faq-category-delete-implementation-plan.md`。
  - 后端新增 `faq/backend/src/category-delete.js`，把分类删除阻断规则抽成纯 helper，并在 `faq/backend/src/index.js` 中复用到单删和新加的 `POST /api/faq/categories/batch-delete`。
  - 前端 `faq/frontend/src/App.jsx`、`faq/frontend/src/App.css` 已补齐分类管理页的删除按钮、勾选框、批量删除入口与结果提示。
  - 已完成最小验证：
    - `cd faq/backend && npm test -- tests/category-delete.test.js` 通过，`5/5`
    - `cd faq/backend && node --check src/index.js` 通过
    - `cd faq/frontend && npm run build` 通过
  - 已完成真实接口 smoke：
    - `AUTH_BASE='http://localhost:5180' API_BASE='http://localhost:5186' npm test -- tests/smoke.e2e.test.js -t "should enforce category delete rules and support partial batch delete"` 通过
  - 运行态排障结论：首次 smoke 失败不是代码逻辑问题，而是 `docker compose up -d --build ...` 后 `faq-api`/`web-faq` 仍复用了旧容器；补做 `docker compose up -d --build --force-recreate --no-deps faq-api web-faq` 后，容器内源码与本地一致，smoke 转绿。
  - FAQ 列表序号功能：
    - 新增设计文档与实现计划：`docs/plans/2026-03-10-faq-article-row-number-design.md`、`docs/plans/2026-03-10-faq-article-row-number-implementation-plan.md`
    - 在 `faq/frontend/src/App.jsx` 按 `((page - 1) * limit) + rowIndex + 1` 计算序号，FAQ 列表与回收站统一采用全局连续编号
    - 在 `faq/frontend/src/App.css` 增加 FAQ 列表序号列宽与居中样式
    - `cd faq/frontend && npm run build` 通过
  - FAQ 分类强制删除功能：
    - 新增设计文档与实现计划：`docs/plans/2026-03-10-faq-category-force-delete-design.md`、`docs/plans/2026-03-10-faq-category-force-delete-implementation-plan.md`
    - 后端在 `faq/backend/src/category-delete.js` 增加分类子树收集与强制删除结果汇总 helper，在 `faq/backend/src/index.js` 新增 `POST /api/faq/categories/:id/force-delete`
    - 强制删除口径已固定：仅 `admin` 可用；递归删除子分类；关联 FAQ 统一移入回收站；FAQ 的 `category_id` 清空；已有回收站 FAQ 仅做 `category_id = null` 脱钩
    - 前端在 `faq/frontend/src/App.jsx` 分类管理行内新增 admin-only“强制删除”按钮，并明确提示“递归删除子分类 + FAQ 进回收站 + 恢复后为未分类”
    - 已完成验证：
      - `cd faq/backend && npm test -- tests/category-delete.test.js` 通过，`7/7`
      - `cd faq/frontend && npm run build` 通过
      - `ADMIN_LOGIN=... ADMIN_PASSWORD=... AUTH_BASE='http://localhost:5180' API_BASE='http://localhost:5186' npm test -- tests/smoke.e2e.test.js -t "should force delete category tree and recycle linked faq articles"` 通过
      - `ADMIN_LOGIN=... ADMIN_PASSWORD=... AUTH_BASE='http://localhost:5180' API_BASE='http://localhost:5186' npm test -- tests/smoke.e2e.test.js -t "should enforce category delete rules and support partial batch delete"` 通过，确认普通删除未回归

## 风险/阻塞

- `tender` 当前剩余主缺口仍是“真实 embedding 服务替换”，今天没有继续往下实现。
- `train-exam` 管理员结果中心虽然已完成构建、测试和容器重启，但尚未做带真实管理员/普通用户账号的交互式人工 smoke。
- `daily-memory` skill 约定的 `scripts/generate_daily_memory.py` 在当前仓库不存在，今天的记忆文件是按模板手工维护的。

## 明日计划

- `tender`：继续推进真实 embedding 服务替换，补上与现有语义检索链路的集成验证。
- `train-exam`：用真实账号走一轮管理员结果列表、查看卷面、查看考生记录，以及普通用户“成绩与证书”入口的人工 smoke。
- 如果人工 smoke 暴露权限或文案问题，优先做最小修复并复跑构建与容器更新。

## 可沉淀为长期记忆

- `train-exam` 管理员结果中心本次运行端口固定为：
  - 前端：`http://localhost:8087`
  - 后端：`http://localhost:5188`
- `tender-api` 容器若再次出现“源码与本地不一致”，当前已验证的有效修复手段是 `--no-cache` 重建后再重启。
- 当前仓库的日记忆文件目录是 `/Users/zhanglei/Documents/codex-new/memory`，但自动生成脚本缺失，需要人工按模板维护。
- `faq` 分类删除当前口径已固定：分类下有 FAQ 不可删，分类下有子分类不可删；批量删除允许部分成功并返回失败列表。
- `docker compose up -d --build` 不一定会让 FAQ 相关服务替换到新容器；如果 smoke 行为和本地代码不一致，需要优先检查容器创建时间与容器内源码，再视情况补 `--force-recreate --no-deps faq-api web-faq`。
- FAQ 列表序号当前口径已固定：按分页全局连续显示，第一页 `1-20`，第二页 `21-40`；回收站与 FAQ 列表保持一致。
- FAQ 分类强制删除当前口径已固定：仅管理员可执行；递归删分类树；关联 FAQ 进入回收站且 `category_id` 置空；普通删除和批量普通删除规则保持不变。
