## 今日对话摘要
用户明确投标系统不是“单一文档生成”，至少需覆盖：招标解析、企业资料匹配、章节装配、规则校验、格式编排、评分优化。用户要求先落地“成稿级校验与评分优化”，并确认“企业资料库=当前自有库”，自动匹配按既有实现继续沿用。用户多次确认方案后，要求我直接执行，不需要他手动操作 worktree。当前回合用户要求先把本线程沉淀成可复用的 md 记忆文件，用于新线程续接。

## 今日产出/进展
已沉淀两份实施文档（此前回合已完成）：
- docs/plans/2026-03-07-tender-final-draft-check-and-score-optimize-design.md
- docs/plans/2026-03-07-tender-final-draft-check-and-score-optimize.md

代码侧已进入 TDD 起步阶段（此前回合已创建）：
- tender/backend/tests/final-draft-registry.test.js
- 覆盖 `buildRequirementRows` / `buildEvidenceRows` / `buildDraftSectionRows` 三个映射函数

本回合核验结果：
- 已确认主工作区存在用户既有脏改，按“增量实现、不中断现有链路”策略推进
- 已跑单测：`tests/final-draft-registry.test.js` 当前失败，原因是缺少模块 `tender/backend/src/final-draft-registry.js`
- 已确认后端现有 analyze/create 主链路、`analysis_summary_json`、`stage_outputs.evidence_registry` 等接入点可扩展

本回合新增：
- 生成并填写今日记忆文件：`memory/2026-03-07.md`

## 风险/阻塞
- 仓库当前为脏工作区（多模块 Dockerfile 与 tender 前后端均有未提交改动），实现时需严格避免误改/回退他人变更。
- `tender/backend/src/index.js` 体量较大，新增成稿校验与评分优化时需要控制改动面，优先模块化到新文件后再接线。
- `daily-memory` skill 文档里默认脚本路径写的是仓库 `scripts/`，实际脚本在 skill 目录，已采用实际路径执行。

## 明日计划
1. 完成 `tender/backend/src/final-draft-registry.js`，先让 `final-draft-registry.test.js` 通过。
2. 扩展 `db.js`：新增 requirement/evidence/draft section 落库表（或最小可用列集），并在 analyze/create 接口接入写入与返回。
3. 新增结构化校验模块与接口 `/api/tender/bids/:id/check`，先实现 missing/evidence/score/placeholder 等第一期规则。
4. 补充/更新后端测试（单测+smoke 关键断言），跑最小可行验证后再汇报。

## 可沉淀为长期记忆
- 用户偏好：默认中文、结论要简洁、步骤要可执行。
- 协作偏好：希望“你直接执行”，不希望自己处理 worktree 或复杂 Git 操作。
- 业务优先级：当前阶段只聚焦“成稿级校验 + 评分优化”，并明确“企业资料库即自有库”。
- 架构原则：AI 负责理解/归纳/扩写/表达优化；程序负责匹配/规则校验/模板填充/导出；关键结果必须结构化且可审计。
