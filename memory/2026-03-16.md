# 2026-03-16

## 今日对话摘要
- 完成了 FAQ 文档列表页的一轮主次关系、响应式和交互细节优化。
- 新增一条长期运行约定：每次开始联调、验收或回归前，先把 Docker Compose 容器切到最新镜像/最新构建。

## 今日产出/进展
- 在根 README 中补充“容器刷新约定”，默认执行：
  - `./scripts/deploy/docker-compose-aliyun.sh pull`
  - `./scripts/deploy/docker-compose-aliyun.sh up -d --build`
- 将上述约定沉淀到今日 memory，方便后续检索。
- 为 FAQ 文档列表页整理 `5.3.0` 发布说明，准备按“功能优化”版本线提交。
- 为 FAQ backend 的分类批量删除和回收站彻底删除整理 `5.4.0` 发布说明，准备按“功能优化”继续升版。

## 风险/阻塞
- `daily-memory` skill 提到的 `scripts/generate_daily_memory.py` 当前仓库不存在，本次改为手工按固定模板生成 `memory/2026-03-16.md`。

## 明日计划
- 后续若继续做前端或联调任务，先执行容器刷新，再进入代码修改和验证。
- 如果本次版本提交流程顺利完成，后续继续收拾 FAQ 页面剩余的品牌感与产品气质问题。

## 可沉淀为长期记忆
- 本仓库默认运维前置动作：先 `pull` 最新镜像，再 `up -d --build`，避免误用旧容器导致“代码已改、页面未变”或旧后端逻辑残留。
- FAQ 文档列表这轮优化按“功能优化”处理，应走 `5.2.3 -> 5.3.0`，提交前缀使用 `feat:`。
- FAQ 分类批量删除与回收站彻底删除这轮按“功能优化”处理，应走 `5.3.0 -> 5.4.0`，提交前缀继续使用 `feat:`。
