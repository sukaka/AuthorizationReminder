## 今日对话摘要
用户要求继续完成会话 `019cc96b-f25d-7110-b802-e1a6bbb16a82` 的未完成工作。排查后确认，该会话实际代码在 worktree `/Users/zhanglei/Documents/codex-new/.worktrees/codex/tender-check-score-phase2`，而不是主工作区。继续验证时发现 tender 完整 smoke 只剩 1 条失败：governance 用例里给 editor 伪造了一个数据库中不存在的用户 ID，导致 tender 后端经 auth introspect 校验时返回 401。
随后用户要求停用该 worktree、改在主工作区推进，并切到共享研发分支 `codex/4.0.6`。在 backlog 盘点后，继续补完 `GAP-0021`，把标书管理页补成项目生命周期管理页。

## 今日产出/进展
- 读取并确认旧会话、计划文档、worktree 状态，识别出当前未完成项已经收缩为 smoke 回归收口。
- 在正确 worktree 上以 `COMPOSE_PROJECT_NAME=codex-new ./scripts/tests/tender.sh` 重建并复用同一组 Docker 服务，拿到真实 smoke 结果。
- 定位 smoke 根因：tender 后端鉴权依赖 `auth` 服务 `/api/auth/introspect`，不能接受不存在用户的测试 token。
- 修改 `tender/backend/tests/smoke.e2e.test.js`：
  - governance 用例不再使用随机 `editorId`
  - 新增通过 MySQL 读取真实 `editor` 账号的辅助逻辑
  - 继续使用共享 JWT secret 生成测试 token，但绑定到真实存在且激活的 `editor` 用户
- 验证结果：
  - 单条回归：`npx vitest run tests/smoke.e2e.test.js -t 'governance metadata'` 通过
  - 全量 tender smoke：`npx vitest run tests/smoke.e2e.test.js` 5/5 通过
  - 此前已确认：
    - `tests/final-draft-registry.test.js` 通过
    - `tests/final-draft-checks.test.js` 通过
    - `tests/score-optimization.test.js` 通过
    - `node --check tender/backend/src/index.js` 通过
    - `npm --prefix tender/frontend run build` 通过
- 在主工作区 `codex/4.0.6` 上继续实现 `GAP-0021`：
  - 新增设计文档 `docs/plans/2026-03-08-tender-project-lifecycle-management-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-project-lifecycle-management-implementation-plan.md`
  - 新增前端纯函数模块 `tender/frontend/src/bid-workflow.js` 和测试 `tender/frontend/src/bid-workflow.test.js`
  - 扩展 `tender/frontend/src/App.jsx`，为选中项目增加基础信息编辑、生命周期进度、成员分派、审核记录面板
  - 扩展 `tender/frontend/src/App.css`，补齐项目生命周期页样式
- 本轮验证结果：
  - `node --test tender/frontend/src/bid-workflow.test.js` 4/4 通过
  - `npm --prefix tender/frontend run build` 通过
- 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0021` 从 `IN_PROGRESS` 更新为 `DONE`
- 继续完成 `GAP-0022` 项目级解析工作台，前后端一起落地：
  - 新增设计文档 `docs/plans/2026-03-08-tender-parse-workspace-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-parse-workspace-implementation-plan.md`
  - 新增后端纯函数模块 `tender/backend/src/parse-workspace.js` 和测试 `tender/backend/tests/parse-workspace.test.js`
  - 在 `tender/backend/src/db.js` 新增 `tender_bid_parse_jobs/files/clauses/tables/matches` 五张项目级解析表
  - 在 `tender/backend/src/index.js` 新增项目级解析 API：
    - `GET /api/tender/bids/:id/parse/workspace`
    - `POST /api/tender/bids/:id/parse/files`
    - `DELETE /api/tender/bids/:id/parse/files/:fileId`
    - `POST /api/tender/bids/:id/parse/files/:fileId/sheets/select`
    - `POST /api/tender/bids/:id/parse/start`
    - `GET /api/tender/bids/:id/parse/jobs/:jobId`
    - `PUT /api/tender/bids/:id/parse/clauses/bulk`
    - `POST /api/tender/bids/:id/parse/matches/recommend`
    - `PUT /api/tender/bids/:id/parse/matches/bulk`
  - 解析工作台后端能力：
    - 支持 `doc/docx/pdf/xls/xlsx/zip`
    - `zip` 自动递归提取 `pdf/doc/docx/xls/xlsx`
    - `xls/xlsx` 支持 sheet manifest 和只解析勾选 sheet
    - 主文件/澄清/附件按项目归档并执行澄清优先字段合并
  - 新增前端纯函数模块 `tender/frontend/src/parse-workspace.js` 和测试 `tender/frontend/src/parse-workspace.test.js`
  - 扩展 `tender/frontend/src/App.jsx`，在项目生命周期面板内接入：
    - 上传解析区
    - ZIP 文件树与 Excel sheet 勾选
    - 解析汇总
    - 条款分类编辑
    - 资产匹配建议/确认/替换/忽略
  - 扩展 `tender/frontend/src/App.css`，补齐解析工作台样式
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0022` 更新为 `DONE`
- 本轮新增验证结果：
  - `cd tender/backend && npx vitest run tests/parse-workspace.test.js` 6/6 通过
  - `node --test tender/frontend/src/parse-workspace.test.js tender/frontend/src/bid-workflow.test.js` 8/8 通过
  - `node --check tender/backend/src/index.js` 通过
  - `node --check tender/backend/src/parse-workspace.js` 通过
  - `npm --prefix tender/frontend run build` 通过
  - `COMPOSE_PROJECT_NAME=codex-new ./scripts/tests/tender.sh` 完成到前端 build/服务就绪，但脚本内置 admin 登录前置检查仍报 400，后续 smoke 输出不稳定，未作为本轮最终验收依据
- 用户确认 `admin` 密码 `Ss544364@` 无误，重新用
  `ADMIN_LOGIN='admin' ADMIN_PASSWORD='Ss544364@' COMPOSE_PROJECT_NAME=codex-new ./scripts/tests/tender.sh`
  验证后，整套 tender 脚本通过：
  - `auth-health`、`tender-api-health`、`tender-web` 就绪
  - `smoke.e2e.test.js` 5/5 通过
  - 之前脚本报 `400 账号或密码错误` 的根因不是密码错误，而是 `scripts/tests/lib/common.sh` 在未显式传入 `ADMIN_PASSWORD` 时会回退到仓库内置旧默认密码
- 用户给出《投标文件 AI 自动生成系统 AI 能力设计说明书》长文档，已完成与当前系统的映射分析：
  - 新增整合说明 `docs/plans/2026-03-08-tender-ai-capability-spec-integration.md`
  - 结论：该文档应作为 tender 系统的 AI 上位设计说明书，而不是替换现有 runtime/module backlog
  - 当前系统已对齐：招标理解、条款分类、知识召回、章节生成、偏离/应答表、优化、风险辅助校验
  - 仍需重点补齐：统一 AI contract (`GAP-0017`)、混合检索与重排 (`GAP-0007/0016`)、评测体系 (`GAP-0019`)、异常重试与接管 (`GAP-0027`)
- 继续完成 `GAP-0023` 项目级初稿工作台，前后端一起落地：
  - 新增设计文档 `docs/plans/2026-03-08-tender-draft-workspace-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-draft-workspace-implementation-plan.md`
  - 新增后端纯函数模块 `tender/backend/src/draft-workspace.js` 和测试 `tender/backend/tests/draft-workspace.test.js`
  - 在 `tender/backend/src/db.js` 新增 `tender_draft_artifact_rows`，用于项目级结构化偏离/应答表持久化
  - 在 `tender/backend/src/index.js` 新增项目级初稿工作台 API：
    - `GET /api/tender/bids/:id/draft/workspace`
    - `PUT /api/tender/bids/:id/draft/sections`
    - `PUT /api/tender/bids/:id/draft/artifacts`
  - 新增前端纯函数模块 `tender/frontend/src/draft-workspace.js` 和测试 `tender/frontend/src/draft-workspace.test.js`
  - 扩展 `tender/frontend/src/App.jsx`，在项目详情下接入：
    - 结构化章节稿编辑
    - 技术/商务偏离表编辑
    - 技术/商务应答表编辑
    - 评分覆盖矩阵与校验问题查看
    - 评分优化触发
    - 项目级存稿与回滚
    - OnlyOffice 快捷打开
  - 扩展 `tender/frontend/src/App.css`，补齐初稿工作台样式
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0023` 更新为 `DONE`
- 本轮新增验证结果：
  - `cd tender/backend && npx vitest run tests/draft-workspace.test.js tests/parse-workspace.test.js` 9/9 通过
  - `node --check tender/backend/src/index.js` 通过
  - `node --check tender/backend/src/draft-workspace.js` 通过
  - `node --test tender/frontend/src/draft-workspace.test.js tender/frontend/src/parse-workspace.test.js tender/frontend/src/bid-workflow.test.js` 11/11 通过
  - `npm --prefix tender/frontend run build` 通过
  - `ADMIN_LOGIN='admin' ADMIN_PASSWORD='Ss544364@' COMPOSE_PROJECT_NAME=codex-new ./scripts/tests/tender.sh` 在本轮 draft workspace 改动后重新回归，最终输出 `[OK] tender 系统测试通过`
- 继续完成 `GAP-0024` 风险中心、模板中心、导出中心，前后端一起落地：
  - 新增设计文档 `docs/plans/2026-03-08-tender-ops-centers-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-ops-centers-implementation-plan.md`
  - 新增后端纯函数模块 `tender/backend/src/ops-center.js` 和测试 `tender/backend/tests/ops-center.test.js`
  - 在 `tender/backend/src/db.js` 新增 `tender_bid_export_records`，用于项目级 Word/PDF/导出包产物记录、状态和下载留痕
  - 在 `tender/backend/src/index.js` 新增运营中心与导出 API：
    - `GET /api/tender/risk-center/summary`
    - `GET /api/tender/export-center/summary`
    - `POST /api/tender/bids/:id/export`
    - `GET /api/tender/export-records/:id/download`
  - 导出能力本轮支持：
    - 基于当前草稿/当前版本导出 `DOCX`
    - 基于 LibreOffice 转换导出 `PDF`
    - 打包导出 `ZIP`，包含 Word、PDF 和风险报告
    - 导出成功后写留痕；若项目当前状态为 `EXPORT_READY`，自动推进到 `EXPORTED`
  - 新增前端纯函数模块 `tender/frontend/src/ops-center.js` 和测试 `tender/frontend/src/ops-center.test.js`
  - 扩展 `tender/frontend/src/App.jsx`，新增三个独立运营页：
    - 风险中心：项目风险总览、来源摘要、推荐动作、打开项目快捷入口
    - 模板中心：docx 模板、模板字段、模板片段、模板包统一管理
    - 导出中心：项目导出列表、Word/PDF/导出包按钮、最近导出记录下载
  - 扩展 `tender/frontend/src/App.css`，补齐运营中心样式
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0024` 更新为 `DONE`
- 本轮新增验证结果：
  - `cd tender/backend && npx vitest run tests/ops-center.test.js tests/draft-workspace.test.js tests/parse-workspace.test.js` 13/13 通过
  - `node --check tender/backend/src/index.js` 通过
  - `node --check tender/backend/src/ops-center.js` 通过
  - `node --test tender/frontend/src/ops-center.test.js tender/frontend/src/draft-workspace.test.js tender/frontend/src/parse-workspace.test.js tender/frontend/src/bid-workflow.test.js` 16/16 通过
  - `npm --prefix tender/frontend run build` 通过
  - `ADMIN_LOGIN='admin' ADMIN_PASSWORD='Ss544364@' COMPOSE_PROJECT_NAME=codex-new ./scripts/tests/tender.sh` 在本轮 ops centers 改动后重新回归，最终输出 `[OK] tender 系统测试通过`
- 继续完成 `GAP-0017` 统一 AI task contract / prompt contract：
  - 新增设计文档 `docs/plans/2026-03-08-tender-ai-task-contract-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-ai-task-contract-implementation-plan.md`
  - 新增 OpenAPI/YAML 契约文档 `docs/swagger/tender-ai-task-contracts.yaml`
  - 将 tender AI contract 明确拆成五类 API family：
    - `parse`
    - `match`
    - `generate`
    - `validate`
    - `export`
  - 将上层逻辑任务明确为 9 个：
    - `PARSE_PROJECT_OVERVIEW`
    - `CLASSIFY_CLAUSES`
    - `EXTRACT_SCORE_ITEMS`
    - `MATCH_ENTERPRISE_ASSETS`
    - `GENERATE_SECTION_DRAFT`
    - `GENERATE_RESPONSE_TEXT`
    - `OPTIMIZE_DRAFT_CONTENT`
    - `EXPLAIN_RISK_FLAGS`
    - `EXPORT_PACKAGE_SUMMARY`
  - 统一了所有 AI 任务必须返回的 envelope 字段：
    - `task_id`
    - `task_type`
    - `status`
    - `result`
    - `confidence`
    - `evidence_sources`
    - `risk_flags`
    - `need_manual_review`
    - `trace`
  - 统一了 prompt contract 最小字段：
    - `prompt_template_id`
    - `prompt_template_version`
    - `output_schema_id`
    - `must_rules`
    - `must_not_rules`
    - `manual_review_when`
  - 明确了逻辑任务到当前运行时的映射，保持现有 `BID_ANALYZE_STAGE1/2/3`、`BID_COMPOSE_DRAFT`、`REWRITE`、`PROOFREAD` 不立即重构，只在其上增加统一 contract 层
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0017` 更新为 `DONE`
- 继续完成 `GAP-0026 / GAP-0027` 治理与异常闭环：
  - 新增后端纯函数模块 `tender/backend/src/governance.js`
  - 新增后端测试 `tender/backend/tests/governance.test.js`
  - 将权限矩阵从“仅 permission + data_scope”扩展为：
    - `menu_permissions`
    - `page_permissions`
    - `button_permissions`
  - `bootstrap` 现在直接返回统一权限摘要，前端可以基于同一份数据做菜单/页面/按钮可见性控制
  - `governance` 现在直接返回当前角色的菜单/页面/按钮权限，以及全量 `permission_matrix`
  - 全局错误处理中新增失败留痕：
    - 所有 `/api/tender/*` 失败请求统一写入 `tender_operation_logs`
    - 动作名为 `REQUEST_FAIL`
    - 保留 `status/code/category/retryable/manual_takeover/details`
  - 将两类瞬时故障显式定义为可重试：
    - AI 上游 `429/5xx/timeout`
    - LibreOffice 转换启动失败、转换失败、转换产物缺失
  - `smoke.e2e.test.js` 已增强为校验治理元数据中的菜单/页面/按钮权限
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0026`、`GAP-0027` 更新为 `DONE`
- 继续完成 `GAP-0007 / GAP-0016` 语义检索与混合召回：
  - 新增设计文档 `docs/plans/2026-03-08-tender-semantic-retrieval-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-semantic-retrieval-implementation-plan.md`
  - 新增后端纯函数模块 `tender/backend/src/semantic-retrieval.js`
  - 新增后端测试 `tender/backend/tests/semantic-retrieval.test.js`
  - 新增前端归一化模块 `tender/frontend/src/semantic-retrieval.js`
  - 新增前端测试 `tender/frontend/src/semantic-retrieval.test.js`
  - parse workspace 的匹配推荐从纯 `RULE` 升级为：
    - 项目资产 `tender_assets + OCR`
    - 知识库切块 `kb_asset_chunks`
    - 知识库 fallback chunk：`kb_section_assets / kb_project_cases / kb_product_specs / kb_company_qualifications / kb_personnel_assets`
  - 新增 chunk 级统一候选结构，推荐结果现在写回：
    - `match_source`
    - `payload.rule_score`
    - `payload.semantic_score`
    - `payload.rerank_score`
    - `payload.chunk_preview`
    - `payload.need_manual_review`
    - `payload.manual_review_reasons`
  - 前端解析工作台已展示：
    - `规则匹配 / 语义召回 / 混合召回`
    - 语义分、规则分、重排分
    - 人工复核标记与原因
    - chunk 预览和来源标签
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0007`、`GAP-0016` 更新为 `DONE`
- 继续完成 `GAP-0013 / GAP-0015` 历史项目知识库沉淀与统一 chunk/tag 标准：
  - 新增设计文档 `docs/plans/2026-03-08-tender-kb-ingestion-design.md`
  - 新增实现计划 `docs/plans/2026-03-08-tender-kb-ingestion-implementation-plan.md`
  - 新增后端纯函数模块 `tender/backend/src/kb-ingest.js`
  - 新增后端测试 `tender/backend/tests/kb-ingest.test.js`
  - 新增前端归一化模块 `tender/frontend/src/kb-ingest.js`
  - 新增前端测试 `tender/frontend/src/kb-ingest.test.js`
  - 在 `tender/backend/src/index.js` 新增项目级知识库沉淀 API：
    - `GET /api/tender/bids/:id/kb/workspace`
    - `POST /api/tender/bids/:id/kb/ingest`
  - 入库链路当前支持：
    - 从已创建项目读取最新解析结果、当前章节稿、项目附件 OCR
    - 生成/刷新 `kb_projects`
    - 重建 `kb_tender_clauses`
    - 重建 `kb_score_items`
    - 重建 `kb_section_assets`
    - 重建 `kb_asset_chunks`
    - 写入 `kb_ingest_jobs`
    - 回写 `tender_bids.source_kb_project_id`
    - 回写 `tender_draft_section_registry.source_kb_section_asset_id`
  - 统一 chunk/tag 标准当前已落地：
    - chunk type：`PROJECT_SUMMARY / CLAUSE_TEXT / SECTION_PARAGRAPH / TABLE_SUMMARY / TABLE_ROW / ATTACHMENT_OCR`
    - 统一 tags：项目标签、状态标签、条款类型标签、附件类型标签、OCR 证据标签
    - 统一质量分：按条款/章节/表格/附件类型给出确定性 `quality_score`
    - 统一复用标记：`reusable_flag`
  - 前端在项目生命周期页新增“知识库沉淀”卡片，支持：
    - 查看可入库条款/评分项/章节/表格/附件数量
    - 编辑项目类型、行业、区域、结果、金额、标签、备注
    - 查看最近沉淀任务历史
    - 一键执行知识库沉淀
  - 本轮验证结果：
    - `cd tender/backend && npx vitest run tests/kb-ingest.test.js` 3/3 通过
    - `node --test tender/frontend/src/kb-ingest.test.js tender/frontend/src/parse-workspace.test.js tender/frontend/src/draft-workspace.test.js tender/frontend/src/semantic-retrieval.test.js` 12/12 通过
    - `node --check tender/backend/src/kb-ingest.js` 通过
    - `node --check tender/backend/src/index.js` 通过
    - `npm --prefix tender/frontend run build` 通过
    - `ADMIN_LOGIN='admin' ADMIN_PASSWORD='Ss544364@' COMPOSE_PROJECT_NAME=codex-new ./scripts/tests/tender.sh` 通过，`smoke.e2e.test.js` 6/6 通过
  - 已将 `docs/requirements/tender-gap-backlog.md` 中 `GAP-0013`、`GAP-0015` 更新为 `DONE`

## 风险/阻塞
- 当前 `auth` 库中的内置账号密码已经不是 docker-compose 里声明的默认密码，`admin/editor` 直接账号密码登录并不稳定，且会触发登录限流。
- worktree 里仍有大量未提交改动；本次只收口了剩余 smoke 红灯，没有做提交、拆分或清理。
- tender 长链路 smoke 依赖本地 Docker 服务、OnlyOffice、auth 与 AI 相关链路，验证耗时约 2 分钟。
- 前端项目仍是单文件 `App.jsx` 为主，后续继续做 `GAP-0022 ~ GAP-0024` 时，复杂度会上升，最好逐步抽模块而不是继续堆在一个文件里。
- 成员分派当前使用“用户名手工录入”模式，因为现阶段没有可复用的用户目录接口供前端选择。
- 当前 `tender.sh` 脚本里的 admin 登录前置检查和实际 auth 库密码状态不一致，导致集成脚本的 smoke 段输出不稳定；本轮功能验收主要依赖代码级校验、单测和前端 build。
- `GAP-0023` 目前完成的是“项目级初稿工作台”，但章节编辑仍是结构化 registry 编辑，不会即时反写到物理 docx；真正的 Word 内容编辑仍以 OnlyOffice 为准。
- 新增 draft workspace API 目前主要由代码级测试、语法检查和前端构建验证，尚未单独补一条新的后端 smoke/e2e 用例覆盖这 3 个接口。
- `GAP-0024` 当前虽已完成运营页和导出记录，但导出能力暂未补独立 e2e；目前验证仍以纯函数测试、后端语法检查、前端构建为主。
- `GAP-0017` 本轮完成的是“文档级 contract 定稿”，还没有把统一 envelope 和 trace 字段真正写进运行时代码；后续实现要按新计划继续推进。
- `GAP-0026 / GAP-0027` 本轮虽然已经收口，但前端目前仍主要消费 `can_*` 布尔字段；新增的 `menu/page/button` 权限摘要已经从后端给出，后续可逐步让前端从布尔散点过渡到统一权限表。
- `GAP-0007 / GAP-0016` 当前完成的是“本地 hybrid semantic retrieval 第一版”，语义分仍是本地轻量 scorer，不是外部 embedding 服务；后续如果补 `GAP-0013 / GAP-0015 / GAP-0019`，可以在不改外层 contract 的情况下替换为真实向量召回。
- `GAP-0013 / GAP-0015` 当前已完成第一版项目级知识库沉淀，但还没有覆盖“离线目录批量导入历史项目”这类更重的 ingest 流程；当前入口是“从已创建项目一键沉淀”。

## 明日计划
- 再往下推进 `GAP-0019`：评测集和 KPI 流水线，直接复用本轮刚定下来的 `task_id / task_type / output_schema_id / prompt_template_version` 作为评测维度。
- 下一步优先推进 `GAP-0019`：把 parse / match / generate / optimize / risk explain 这些任务都接到统一评测样本和 KPI 汇总。
- 若继续做项目管理深化，可补审核动作处理页和项目成员选择器，但这需要新的用户目录接口支持。
- 如需交付，整理本次前端生命周期管理改动并提交到 `codex/4.0.6`。

## 可沉淀为长期记忆
- 续接旧会话时，先确认真实工作目录；本次有效代码在 `.worktrees/codex/tender-check-score-phase2`，不是主工作区。
- tender smoke 依赖 Docker 服务，应优先从正确 worktree 根目录运行；若要覆盖主工作区容器，使用 `COMPOSE_PROJECT_NAME=codex-new`。
- tender 后端鉴权依赖 `auth` introspect，测试 token 必须绑定到数据库中真实存在且激活的用户，不能再用随机用户 ID 伪造。
- tender 项目管理页当前已补齐基础信息编辑、生命周期进度、成员分派、审核记录四块；成员分派暂时依赖用户名手工录入，不依赖用户目录接口。
- tender 项目级解析工作台当前已接通前后端：支持主文件/澄清/附件联合上传、ZIP 递归解压、sheet 勾选、条款分类和资产匹配确认，但仍未打通到 `bid-generate` 主链路。
- tender AI 能力长文档应作为“AI 上位设计说明书”使用，而不是替代 backlog/runtime 文档；它最适合拿来约束统一 AI task contract、trace、评测和人工复核边界。
- tender 项目级初稿工作台当前已接通前后端：可在已创建项目下编辑结构化章节稿、偏离/应答表，执行评分优化和成稿校验，并管理存稿与回滚；但结构化章节编辑不会直接反写物理 docx。
- tender `GAP-0024` 当前已完成：新增风险中心、模板中心、导出中心三个独立运营页；后端提供项目级风险/导出聚合 API 和导出记录下载，导出支持 Word/PDF/导出包。
- tender `GAP-0017` 当前已完成：统一 AI contract 的设计文档、实现计划和 OpenAPI/YAML schema 已补齐；后续代码实现不要再新增散乱 AI 出参，统一收敛到 `task_id/task_type/result/confidence/evidence_sources/risk_flags/need_manual_review/trace` 这组 envelope。
- tender `GAP-0026 / GAP-0027` 当前已完成：后端已统一输出菜单/页面/按钮权限摘要，并在全局错误处理中对 tender 失败请求写入治理日志；AI 上游和文件转换类瞬时故障已带 `retryable + manual_takeover`。
- tender `GAP-0007 / GAP-0016` 当前已完成：parse workspace 匹配推荐已升级为 hybrid recall，返回规则分、语义分、重排分、来源和人工复核标记；当前是本地 semantic scorer，后续可平滑替换为真实 embedding pipeline。
- tender `GAP-0013 / GAP-0015` 当前已完成：知识库沉淀入口挂在项目详情页下，后端会把最新解析结果、章节稿和附件 OCR 标准化刷新到 `kb_projects / kb_tender_clauses / kb_score_items / kb_section_assets / kb_asset_chunks / kb_ingest_jobs`，并统一 tags、chunk_type、quality_score、reusable_flag。
