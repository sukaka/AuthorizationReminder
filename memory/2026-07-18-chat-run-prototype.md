# 聊天 Run 交互原型

## 当前工作

- 根据 Codex 对标审计方案新增独立聊天原型页面。
- 原型验证统一 Run 的消息活动卡片、计划/活动/来源/成果侧栏、Steer 和 Queue 操作。

## 约束

- 原型不接真实后端，仅使用本地演示状态；`?prototype=chat` 会绕过真实登录检查，仅用于本地预览。
- 不改变真实 `ChatPage` 的运行逻辑。
- 不升级版本、不提交、不推送。

## 访问方式

- 启动桌面端 Web 开发服务后访问 `http://localhost:18093/?prototype=chat`。
