
## 晚间继续 - Agent 独立版本与更新发布（Task 3-9）

- Task 3：渠道锁定 Tauri 配置与内网测试包。新增 `workspace-private-http` capability、`build-macos-lan-test-arm64.sh`/`build-windows-lan-test-x64.ps1` 构建脚本、render-tauri-config 支持 `buildMode`、LauncherPage 显示渠道标签和 HTTP 警告。
- Task 4：更新发布数据库模型。新增 `ai_desktop_update_releases`/`ai_desktop_update_artifacts` 表、0004 迁移、`desktop_update_storage_dir`/`desktop_update_max_bytes` 配置、卷挂载。
- Task 5：不可变更新产物上传与发布领域服务。`create_release`、`store_artifact`（流式 SHA 复算）、`publish_release`（生产需双平台）、`withdraw_release`（不删除产物）。SemVer 强制、版本单调、名称约束。
- Task 6：管理员发布 API（`POST/GET /admin/desktop-updates`、上传产物、发布/撤回）和公开 Tauri 清单/下载接口（`/desktop/updates/{channel}/{target}/{arch}/latest.json`、Range 下载）。
- Task 7：治理中心"桌面端更新"页面。创建草稿、上传产物、发布/撤回流程，仅 admin/sysadmin 可见。
- Task 8：Tauri 更新产物清单生成器 `create-updater-manifest.mjs`，收集 `.app.tar.gz`+`.sig`（拒绝 DMG），禁止 secret 字段。
- Task 9：并发发布测试 `test_desktop_update_concurrency.py`，验证版本单调选择。

### 验证状态
- 交付测试：67/67 通过
- 前端测试：56/56 通过（build-mode + launcher-page）
- TypeScript：零错误
- render-tauri-config 测试：19/19 通过

### 待完成
- 后端 pytest 全量测试（需 Docker 环境或本地 pip 依赖）
- Rust cargo test/clippy（需 Rust 工具链）
- macOS/Windows 真实构建验收（Task 10）
- 推送到 origin（需 GitHub `workflow` scope PAT）
