## 今日对话摘要

- 核查了当前整套系统的数据库部署方式，确认 MySQL 常驻运行实例只有一个容器：`codex-new-mysql-1`。
- 进一步按系统梳理了各后端服务连接的数据库名、运行账号、端口和是否共库。

## 今日产出/进展

- 已通过 `docker compose ps` 确认当前只有一个运行中的 MySQL 服务：`mysql`。
- 已通过 `SHOW DATABASES` 确认该单实例内承载的业务库包括：
  - `cmdb`
  - `juxin_reminder`
  - `juxin_inventory`
  - `juxin_device_flow`
  - `juxin_sec_impl`
  - `juxin_faq`
  - `juxin_tender`
  - `juxin_train_exam`
- 已通过 `mysql.user` 和 `SHOW GRANTS` 核对主要业务账号与授权范围：
  - `auth_user -> juxin_reminder`
  - `faq_user -> juxin_faq`
  - `train_exam_user -> juxin_train_exam`
  - `tender_user -> juxin_tender`
  - `sec_impl_user -> juxin_sec_impl`
  - `juxin -> juxin_reminder / juxin_inventory / juxin_device_flow`
- 已新增一份长期可复用文档：
  - `docs/manuals/system-mysql-topology.md`
  - 文档已从单纯 MySQL 说明升级为“系统运行架构总览（含 MySQL 拓扑）”，增加了系统级前后端、端口、中间件依赖和特殊共享关系表
  - 文档已补充 Mermaid 系统级架构图，可直接从文档中查看“接入层 -> 服务层 -> 基础设施层”的整体依赖关系

## 可沉淀为长期记忆

- 当前仓库 `docker-compose.yml` 中只有一个常驻 MySQL 服务：`mysql`
- `cmdb-mysql-init` 不是第二个数据库实例，只是一次性的初始化任务容器
- 现状是“单 MySQL 容器 + 多业务库”，不是“每系统一个 MySQL 容器”
- 培训考试系统除了自己的 `juxin_train_exam` 库，还会额外连接 FAQ 的 `juxin_faq` 库
- CMDB 当前通过 DSN 直接使用 `root` 连接 `cmdb` 库，隔离性相对最弱
- 当前架构总览文档主路径为 `docs/manuals/system-mysql-topology.md`，虽然文件名保留了 MySQL 字样，但内容已经覆盖整套系统运行架构
- 架构图当前采用系统级抽象，不展开所有容器端口细节；其中 `train-exam-api` 访问 FAQ 库的附加连接只在图下注释说明
