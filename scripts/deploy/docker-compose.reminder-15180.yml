services:
  auth:
    environment:
      APP_REMINDER_URL: "http://${PUBLIC_HOST:-localhost}:18080"
    ports:
      - "15180:5180"
  web:
    build:
      args:
        VITE_SSO_PORTAL_URL: "http://${PUBLIC_HOST:-localhost}:15180"
