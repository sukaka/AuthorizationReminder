# 5.70.2

## Bug 修复

- 修复本地预览未登录时多个大屏模板空白的问题。
- `localhost`、`127.0.0.1`、`::1` 下数据接口失败且尚无真实数据时，播放器自动使用本地演示数据，并在健康状态处标记为 `MOCK`。
- 非本地域名仍保留真实错误状态，避免生产环境被演示数据掩盖。

## 验证

- 前端数据通道和本地预览回退测试通过。
- 前端类型检查和生产构建通过。
- 已在 `18092` 验证 `/play/sca-03` 和 `/play/train-01` 不再空屏。
