# 5.8.0

## 本次更新

- 发送配置页改为分块保存，不再使用单一总保存按钮
- 邮箱、短信、OCR、企业微信、发送控制、提醒模板都拥有各自独立的保存按钮
- 测试邮箱、测试短信、测试企业微信只会检查对应卡片是否存在未保存改动，不再被其它配置块牵连

## 部署说明

- 本次仅涉及提醒系统前端 `web`
- 更新代码后重建 `web` 服务即可生效

## 验证建议

- 分别修改邮箱、短信、OCR、企业微信配置，确认每块都能独立保存
- 修改短信配置但不保存，测试邮箱时不应再被短信配置拦住
- 修改邮箱配置但不保存，测试邮箱时仍应提示先保存邮箱配置
