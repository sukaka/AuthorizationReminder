# v5.10.5

## 变更摘要

- 批量导入用户时不再发送个人邮件或 admin 汇总邮件
- 导入用户初始密码统一改为 `!b$#+^o9uF`
- 保留首次登录强制修改密码
- 导入结果文件仍保留 `initial_password` 列，邮件状态列改为空值

## 影响范围

- 管理后台用户导入接口
- 用户导入结果 Excel

## 验证

```bash
node --test server/tests/user-import.test.js auth/tests/admin-center-user-import.test.js
node --check server/user-import.js
node --check auth/index.js
```
