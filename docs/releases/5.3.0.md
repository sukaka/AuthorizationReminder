# 5.3.0 正式版（FAQ 文档列表主次重排与工作台收敛）

发布日期：2026-03-16
标签：`v5.3.0`
分支：`codex/5.3.0`

## 本次版本定位

- 版本类型：功能优化
- 版本升级：`5.2.3 -> 5.3.0`
- 判定依据：
  - FAQ 文档列表页完成一轮可感知的信息架构与交互优化
  - 主任务从“新建 + 管理 + 浏览并列”收敛为“先找文档，再按需创建”
  - 同时补齐移动端字段上下文、空状态 CTA 和容器刷新约定

## 已完成

- FAQ 文档列表页主次关系重排：
  - 快捷区收敛为“继续处理”与“待处理提醒”
  - 筛选区改为带可见标签的工具卡
  - 结果区新增结果概览
- FAQ 新建流程收敛：
  - “新建文档”默认收起
  - 仅在用户准备创建时再展开输入项
  - 空状态新增“新建第一篇文档”入口
- FAQ 列表可用性增强：
  - 修复筛选项在窄宽度下被截断的问题
  - 移动端列表补齐字段标签，避免仅剩值没有上下文
  - 扩大链接/复选框点击区域，补充 `focus-visible`
- 运维约定补齐：
  - 根 README 新增“容器刷新约定”
  - 默认先 `pull` 再 `up -d --build`，减少旧镜像和旧容器残留带来的误判

## 影响范围

- `faq/frontend` 文档列表页
- FAQ 回收站与批量操作列表呈现
- 根 README 的 Docker Compose 运维约定

## 发布注意事项

- FAQ 联调、回归、验收前，默认执行：
  - `./scripts/deploy/docker-compose-aliyun.sh pull`
  - `./scripts/deploy/docker-compose-aliyun.sh up -d --build mysql auth onlyoffice faq-api web-faq`
- 本次主要是前端结构与交互优化，不涉及 FAQ 接口契约变更。

## 本次校验

- `node --test /Users/zhanglei/Documents/codex-new/tests/versioning-automation.test.js`
- `node --test /Users/zhanglei/Documents/codex-new/faq/frontend/tests/source.app.test.cjs`
- `npm run build`（`/Users/zhanglei/Documents/codex-new/faq/frontend`）
- `docker inspect -f '{{.Name}}\t{{.State.Status}}\t{{.State.StartedAt}}' codex-new-mysql-1 codex-new-auth-1 codex-new-onlyoffice-1 codex-new-faq-api-1 codex-new-web-faq-1`
