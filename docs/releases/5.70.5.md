# 5.70.5

发布日期：2026-06-12

## Bug 修复

- 修复统一大屏入口登录守卫误拦截显式演示模式的问题：`/play/*?mock=1` 继续允许进入本地演示数据，普通 `/play/*` 未登录仍跳转统一登录。

## 验证

- `curl -I http://127.0.0.1:18092/play/train-03` 返回 `302`，Location 指向 `http://127.0.0.1:5180/portal?system=big-screen`。
- `curl -I http://127.0.0.1:18092/play/train-03?mock=1` 返回 `200`。
