# 5.7.8

## 本次更新

- 修复发送配置保存成功后，邮箱密码等敏感字段仍被误判为未保存的问题
- 保存成功后，前端会把敏感字段统一回写成服务端同口径的遮罩值 `******`
- 这样发送配置页的“未保存检测”不会再因为明文与遮罩值不一致而卡在 `email.pass`

## 部署说明

- 本次仅涉及提醒系统前端 `web`
- 更新代码后重建 `web` 服务即可生效

## 验证建议

- 在发送配置里重新输入 SMTP 授权码并点击“保存配置”
- 保存成功后立刻点击“测试邮箱”，确认不再出现 `未保存项：email.pass`
