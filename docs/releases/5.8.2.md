# 5.8.2

## 本次更新

- 修复发送配置分块保存后，邮箱密码等敏感字段仍被误判为“未保存”的问题
- 保存邮箱、短信、OCR、企业微信等配置后，会立即以后端回读结果同步前端状态
- 若保存失败，页面会直接显示后端返回的具体错误，不再只提示笼统的“保存失败”

## 部署说明

- 本次涉及提醒系统前端 `web`
- 建议同时升级代码并重建 `web`

## 验证建议

- 在邮箱配置里重新输入 SMTP 授权码并点击“保存邮箱配置”
- 保存成功后立刻点击“测试邮箱”，不应再出现 `未保存项：email.pass`
- 若保存失败，应能直接看到具体错误原因
