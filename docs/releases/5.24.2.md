# 5.24.2

## Bug 修复

- 修复授权到期提醒前端仍显示旧版本 `v5.10.12` 的问题，`web/package.json` 与 `web/package-lock.json` 现在会随整套系统版本同步。
- 版本自动化会强制对齐主系统前端等运行时包，避免历史子包版本落后导致页面显示旧版本。

## 验证

- `node --test tests/versioning-automation.test.js`
- `node --test src/version-display.test.js`（在 `web/` 目录）
- `npm --prefix web run build`
