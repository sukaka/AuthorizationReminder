# 5.6.1

## 本次更新

- 放开内置账号在管理后台中的角色修改限制
- `admin / sysadmin / auditor / editor / reviewer` 账号现在可以按需要调整角色
- 仍然保留内置账号不可删除、不可禁用的保护

## 部署说明

- 本次只涉及统一登录后台用户管理逻辑，重建 `auth` 服务即可生效

## 验证建议

- 在管理后台编辑 `admin` 账号，确认角色下拉可保存成功
- 确认内置账号仍不能被禁用或删除
