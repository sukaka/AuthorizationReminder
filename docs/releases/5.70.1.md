# 5.70.1

## Bug 修复

- 调整 Docker Compose 宿主端口，避免本地已有服务占用导致统一大屏依赖启动失败：
  - MySQL: `53308 -> 3306`
  - SCA PostgreSQL: `55433 -> 5432`
  - SCA Redis: `56380 -> 6379`

## 验证

- `docker compose config --quiet` 通过。
- `big-screen-api` `/health` 与 `/api/big-screen/health` 返回正常。
- `web-big-screen` `18092` 前端入口返回 `200 OK`。
