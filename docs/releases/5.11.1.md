# 5.11.1

## 修复

- 修复新服务器首启时 `ALIYUN_MIRROR_URL` 示例占位符可能被原样写入 `/etc/docker/daemon.json`，导致 Docker 拉取基础镜像时访问无效镜像加速地址的问题。
- 新增部署脚本测试，确保占位符镜像加速地址会被拒绝，且不会写入 Docker daemon 配置。

## 部署提示

- 如果服务器已经写入错误的 `registry-mirrors`，需要先修正或清空 `/etc/docker/daemon.json` 后重启 Docker。
