# 多系统测试执行记录（2026-02-20）

## 执行范围
- Auth/SSO
- Reminder
- Ticketing
- Inventory
- Device Flow
- Sec-Impl
- CMDB

## 执行结果汇总
| 系统 | 测试项 | 结果 | 备注 |
|---|---|---|---|
| Auth/SSO | 登录与 apps 授权读取 | 通过 | `GET /api/auth/apps` 返回 200 |
| Reminder | 健康检查 + dashboard + me | 通过 | `/api/health`、`/api/dashboard`、`/api/auth/me` 均 200 |
| Ticketing | 健康检查 + tickets + SLA groups | 通过 | `/health`、`/api/tickets`、`/api/dashboard/sla-groups` 均 200 |
| Inventory | 健康检查 + dashboard + products + balances | 通过 | 关键读接口均 200 |
| Device Flow | smoke/regression/rbac 脚本 | 通过 | 三套脚本全部通过 |
| Sec-Impl | dashboard/logs 读接口 | 通过 | 关键读接口 200；Vitest 全套此前已通过 |
| CMDB | healthz + ci list | 通过 | `/healthz`、`/api/v1/ci` 均 200 |
| CMDB | dashboard overview | 通过 | 已修复 SQL `ONLY_FULL_GROUP_BY` 兼容问题，现返回 200 |

## 已执行命令（节选）
```bash
docker compose up -d --build mysql auth api ticketing shipping-gateway inventory-api device-flow-api sec-impl-api cmdb-mysql-init cmdb web-cmdb

node --check server/index.js
node --check auth/index.js
node --check ticketing/index.js
node --check inventory-system/backend/src/index.js
node --check device-flow/backend/src/index.js
go test ./...   # cmdb

npm --prefix web run build
npm --prefix ticketing/web run build
npm --prefix inventory-system/frontend run build
npm --prefix device-flow/frontend run build
npm --prefix sec-impl/frontend run build
npm --prefix cmdb/web run build

cd device-flow/scripts
AUTH_TOKEN=<token> API_BASE=http://localhost:5184 ./smoke-e2e.sh
AUTH_TOKEN=<token> API_BASE=http://localhost:5184 ./regression-api.sh
AUTH_TOKEN_ADMIN=<token> AUTH_TOKEN_AUDITOR=<token> AUTH_TOKEN_SYSADMIN=<token> ./rbac-matrix.sh
```

## 问题闭环
- 历史问题：`GET /api/v1/dashboard/overview` 返回 500（`ONLY_FULL_GROUP_BY` 下趋势 SQL 分组表达式不一致）
- 修复文件：`/Users/zhanglei/Documents/codex-new/cmdb/internal/repository/dashboard_repository.go`
- 修复后验证：`GET http://localhost:8090/api/v1/dashboard/overview` 返回 `200` 且返回结构完整
